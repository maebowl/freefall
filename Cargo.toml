[package]
name = "freefall"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
avian2d = "0.5"
rand = "0.9"

# Optimize dependencies in dev builds for playable performance
[profile.dev.package."*"]
opt-level = 2
