[package]
name = "freefall"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
bevy_ecs_ldtk = "0.14"
avian2d = "0.5"

# Optimize dependencies in dev builds for playable performance
[profile.dev.package."*"]
opt-level = 2
