/target

# Runtime/user data written by the game (saved name, zen leaderboard)
.freefall/

# LDtk editor auto-backups
all-the-levels/backups/

# Wrangler local state / node deps (leaderboard worker tooling)
.wrangler/
node_modules/
